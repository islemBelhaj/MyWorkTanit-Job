{% extends 'base.html.twig' %}

{% block title %}Langue{% endblock %}

{% block body %}
    <h1>Langue</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ langue.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ langue.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_langue_index') }}">back to list</a>

    <a href="{{ path('app_langue_edit', {'id': langue.id}) }}">edit</a>

    {{ include('Back/employeur/langue/_delete_form.html.twig') }}
{% endblock %}
