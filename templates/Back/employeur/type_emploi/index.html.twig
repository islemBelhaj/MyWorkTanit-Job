{% extends 'base.html.twig' %}

{% block title %}TypeEmploi index{% endblock %}

{% block body %}
    <h1>TypeEmploi index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_emploi in type_emplois %}
            <tr>
                <td>{{ type_emploi.id }}</td>
                <td>{{ type_emploi.Libelle }}</td>
                <td>
                    <a href="{{ path('app_type_emploi_show', {'id': type_emploi.id}) }}">show</a>
                    <a href="{{ path('app_type_emploi_edit', {'id': type_emploi.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_emploi_new') }}">Create new</a>
{% endblock %}
